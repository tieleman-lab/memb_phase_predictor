name: test
channels:

  - conda-forge

  - defaults
dependencies:
    # Base depends
  - python
  - pip

    # Testing
  - pytest
  - pytest-cov
  - codecov

    # Package dependencies
  - mdanalysis  
  - pandas
  - tqdm
  - numpy
  - scikit-learn
  - hmmlearn
  - lipyphilic

    # Pip-only installs
  #- pip:
  #  - codecov
    

